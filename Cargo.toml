[workspace]
members = [
    "engine",
    "players",
    "local",
    "deck_builder",
    "proto",
    "server",
    "clients",
]

default-members = ["local"]

[profile.release]

[profile.dev]
opt-level = 3
